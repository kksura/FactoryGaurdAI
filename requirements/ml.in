# ML/tracking dependencies (torch is installed separately — see torch.txt).
# Co-resolved with base.in and dev.in into requirements/lock.txt.
scikit-learn>=1.5
mlflow>=2.14
shap>=0.45
matplotlib>=3.9
joblib>=1.4
safetensors>=0.4
